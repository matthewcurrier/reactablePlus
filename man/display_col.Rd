% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table-config.R
\name{display_col}
\alias{display_col}
\title{Build a read-only display column specification}
\usage{
display_col(id, label, width = NULL, min_width = NULL, render_fn = NULL)
}
\arguments{
\item{id}{\code{character(1)}. The name of the column in \code{source_data}
to display. Also used as the column key in the reactable data
frame.}

\item{label}{\code{character(1)}. The column header text.}

\item{width}{Integer or \code{NULL}. Fixed column width in pixels.}

\item{min_width}{Integer or \code{NULL}. Minimum column width.}

\item{render_fn}{Function or \code{NULL}. An optional custom cell
renderer \verb{(value, row_key)} -> HTML string. When \code{NULL}
(the default), values are shown as plain escaped text.}
}
\value{
A \code{display_col} list (S3 class \code{"display_col"}).
}
\description{
Defines a non-editable column whose values come from
\code{source_data} in dynamic mode. Display columns provide context
alongside editable \code{widget_col()} columns — e.g. an email address,
department name, or enrollment date that the user can see but not
edit.
}
\examples{
display_col("email", "Email", width = 200)
display_col("department", "Dept", min_width = 120)
display_col("status", "Status", render_fn = function(value, row_key) {
  cls <- if (identical(value, "active")) "badge-success" else "badge-muted"
  sprintf('<span class="\%s">\%s</span>', cls, value)
})

}
