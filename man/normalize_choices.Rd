% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_col_spec.R
\name{normalize_choices}
\alias{normalize_choices}
\title{Normalize dropdown choices to list-of-lists format}
\usage{
normalize_choices(choices)
}
\arguments{
\item{choices}{A character vector, named character vector, or list of
lists. Empty strings and \code{NA} values in character vectors are rejected.}
}
\value{
A list of lists, each with \code{label} (character) and \code{value}.
}
\description{
Converts shorthand choice formats into the canonical list-of-lists
structure expected by \code{validate_col_spec()} and the cell renderers.
Called automatically during validation, but can also be used directly.
}
\details{
Three formats are accepted:
\describe{
\item{Unnamed character vector}{\code{c("High", "Medium", "Low")} —
label and value are identical.}
\item{Named character vector}{\code{c("High" = "high", "Medium" = "med")} —
names become labels, values become values.}
\item{List of lists}{\code{list(list(label = "High", value = "high"), ...)} —
canonical format, returned as-is.}
}
}
\examples{
# Unnamed vector — label == value
normalize_choices(c("High", "Medium", "Low"))

# Named vector — names are labels
normalize_choices(c("High" = "high", "Medium" = "med", "Low" = "low"))

# Already canonical — returned as-is
normalize_choices(list(
  list(label = "Good", value = 3),
  list(label = "Bad",  value = 1)
))

}
