% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge_existing_data.R
\name{merge_existing_data}
\alias{merge_existing_data}
\title{Merge previously saved values onto the display data frame}
\usage{
merge_existing_data(data_df, existing_data, row_spec, col_spec)
}
\arguments{
\item{data_df}{A tibble containing at least the id_col and display
columns declared in \code{row_spec}.}

\item{existing_data}{A tibble of previously saved values (id_col + col_spec
columns), or \code{NULL} for a blank start.}

\item{row_spec}{A validated row_spec list.}

\item{col_spec}{A validated col_spec list.}
}
\value{
A tibble with exactly the id_col, display_cols, and col_spec columns
— in that order. One row per row in \code{data_df}.
}
\description{
Left-joins \code{existing_data} onto \code{data_df} using the id column
declared in \code{row_spec}. Rows in \code{data_df} with no match in
\code{existing_data} receive \code{NA} for every col_spec column. Rows
in \code{existing_data} that are absent from \code{data_df} are silently
dropped.
}
