% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/school-picker.R
\name{searchPickerInput}
\alias{searchPickerInput}
\alias{schoolPickerInput}
\title{Search picker input}
\usage{
searchPickerInput(
  inputId,
  value = NULL,
  grade_label = NULL,
  grade_key = NULL,
  show_nces_id = TRUE,
  trigger_label = "+ Pick school",
  popover_title = "Find school",
  search_placeholder = "Search by school name, city, district…",
  empty_hint = "Type 2+ characters to search 100k+ US schools",
  no_match_hint = "No schools match. Check spelling — picking is required.",
  show_fill_down = TRUE,
  ns = "",
  width = "100\%"
)

schoolPickerInput(...)
}
\arguments{
\item{inputId}{Character. The Shiny input ID. The value is a named list
with \code{id}, \code{name}, \code{district}, \code{city}, \code{state}, \code{type}, or \code{NULL}.}

\item{value}{Optional initial value — a list with the school fields above.}

\item{grade_label}{Optional display label (e.g. \code{"3rd grade"}) for
accessible naming.}

\item{grade_key}{The grade's two-char key (e.g. \code{"PK"}, \code{"03"}). Used by
the fill-down action to tell the server which grade triggered it.}

\item{show_nces_id}{Logical. Whether to show the NCES ID in the filled
state and search results. Default \code{TRUE}.}

\item{trigger_label}{Character. Text on the empty-state trigger button.
Default \code{"+ Pick school"}.}

\item{popover_title}{Character. Popover header text.
Default \code{"Find school"}.}

\item{search_placeholder}{Character. Placeholder in the search input.
Default \code{"Search by school name, city, district\\u2026"}.}

\item{empty_hint}{Character. Hint text shown before the user types.
Default \code{"Type 2+ characters to search 100k+ US schools"}.}

\item{no_match_hint}{Character. Text shown when search returns no results.
Default \code{"No schools match. Check spelling \\u2014 picking is required."}.}

\item{show_fill_down}{Logical. Whether to show the "fill down" action
link in the filled state. Default \code{TRUE}.}

\item{ns}{Character. The Shiny module namespace prefix for search
communication. Pass \code{session$ns("")} from within a module. For
top-level apps, leave as \code{""}.}

\item{width}{CSS width. Default \code{"100\%"}.}
}
\value{
An \link[htmltools:tagList]{htmltools::tagList}.
}
\description{
Creates a typeahead search picker widget. When empty, renders a
dashed-border trigger button. When filled, renders a card showing
the selected result's details and action links (change, fill down,
clear).

\code{schoolPickerInput()} is a deprecated alias for
\code{searchPickerInput()}. It will be removed in a future release.
}
\details{
Search is server-side: the widget sends queries to R via
\verb{input$<ns>school_search}; use \code{\link[=useTypeaheadSearch]{useTypeaheadSearch()}} in your
server function to wire up the response.

Trigger and popover labels are configurable so the widget can be
used for any server-side typeahead scenario (school search,
provider lookup, facility search, etc.).
}
\examples{
\dontrun{
# School search (default labels)
searchPickerInput("school_PK", grade_label = "PreK", grade_key = "PK")

# Custom labels for provider search
searchPickerInput("provider_PK",
  trigger_label = "+ Find provider",
  popover_title = "Search providers",
  search_placeholder = "Type provider name or specialty\u2026",
  empty_hint = "Type 2+ characters to search providers",
  no_match_hint = "No providers found.",
  show_nces_id = FALSE,
  show_fill_down = FALSE,
  grade_label = "PreK",
  grade_key = "PK"
)
}

}
