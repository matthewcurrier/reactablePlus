% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dependencies.R
\name{useReactablePlus}
\alias{useReactablePlus}
\alias{useSchoolHistory}
\title{Include all reactablePlus widget dependencies}
\usage{
useReactablePlus()

useSchoolHistory()
}
\value{
An \link[htmltools:tagList]{htmltools::tagList} of HTML dependencies (invisible in the UI).
}
\description{
Drop this into your UI (e.g. inside \code{fluidPage()} or a module's
\code{tagList()}) to ensure all JS and CSS for picker widgets is loaded
at the page level. This is \strong{required} when embedding pickers inside
\code{reactable} cells, because reactable's React-based rendering does
not process \link[htmltools:htmlDependency]{htmltools::htmlDependency} objects returned from cell
functions.

\code{useSchoolHistory()} is a deprecated alias for
\code{useReactablePlus()}. It will be removed in a future release.
}
\details{
Calling this once covers SchoolPicker, AttendancePicker,
HomeschoolPicker, NotesInput, and GearPopover. Duplicate calls are
harmless (htmltools deduplicates).
}
\examples{
\dontrun{
ui <- fluidPage(
  useReactablePlus(),
  reactableOutput("table")
)
}

}
