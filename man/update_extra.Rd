% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cell-extras.R
\name{update_extra}
\alias{update_extra}
\title{Set \verb{*_extra} cell values from the server}
\usage{
update_extra(
  session = shiny::getDefaultReactiveDomain(),
  col_id,
  values,
  ns = ""
)
}
\arguments{
\item{session}{The Shiny session object. Defaults to the current
reactive domain.}

\item{col_id}{\code{character(1)}. The column id used when the cells were
created with a factory such as \code{\link[=text_extra]{text_extra()}}.}

\item{values}{A named list or named vector keyed by \strong{row id}
(matching the cells' \code{data-row}), each entry the new value for that
row. Rows not named are left unchanged.}

\item{ns}{\code{character(1)}. The same namespace prefix passed to the
factory (e.g. \code{session$ns("")} inside a module). Default \code{""}.}
}
\value{
Called for its side effect; returns invisibly.
}
\description{
Pushes authoritative values into the client-side store for an
\verb{*_extra} column, updating both the store and the live DOM. Use this
when the \strong{server} needs to change a cell's value — e.g. to clear a
cell, copy a value down, or reset a column — so the change survives
reactable's repaints instead of being overruled by the store's
"store wins on repaint" reconcile.
}
\examples{
\dontrun{
# Clear row 2 and set row 3, server-side:
update_extra(session, "comment", list(`2` = "", `3` = "set by server"))
}

}
\seealso{
\code{\link[=text_extra]{text_extra()}}, \code{\link[=bindExtrasOnRender]{bindExtrasOnRender()}}
}
