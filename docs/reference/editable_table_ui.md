# UI function for the editable table Shiny module

UI function for the editable table Shiny module

## Usage

``` r
editable_table_ui(id)
```

## Arguments

- id:

  A string. The module namespace ID.

## Value

A named list of `shiny.tag` objects.
