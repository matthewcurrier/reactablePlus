# Grade choices and labels

Constants for the 14-grade ladder from PreK through 12th.

## Usage

``` r
gradeChoices()

gradeLabelMap()
```

## Value

`gradeChoices()` returns a named character vector (values are two-char
keys, names are display labels). `gradeLabelMap()` returns a named list
mapping grade keys to display labels.
